package com.company.example;

public class HashTable {
    // Hashtable
    // put(k,v)
    // get(k) : v
    // remove(k)
    // k: int
    // v: string
    // Collisions: chaining
    // LinkedList<Entry>[]
    // [LL, LL, LL, LL]
}
